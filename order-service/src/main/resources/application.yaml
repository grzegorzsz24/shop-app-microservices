spring:
  application:
    name: order-service
  datasource:
    username: root
    password: mysql
    url: jdbc:mysql://localhost:3316/order_service?autoReconnect=true&createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /api-docs

server:
  port: 8081

inventory:
  url: http://localhost:8082